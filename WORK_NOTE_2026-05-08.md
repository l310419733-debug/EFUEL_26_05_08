# EFUEL 工作备注 2026-05-08

## 本次目标

在 Fast-Drone-250 基础上部署 RACER 单机实机探索流程，并将当前可运行工作区纳入本地 Git 管理，便于后续回退和继续调试。

## 已完成内容

- 在 `/home/lyz/EFUEL` 建立本地 Git 仓库，并完成第一次完整工作区快照。
- 保留 Fast 实机基础模块，接入 RACER 单机探索相关规划、建图、RViz 和 launch 配置。
- 实机探索入口使用 `roslaunch exploration_manager exploration.launch`。
- RViz 入口使用 `roslaunch exploration_manager rviz.launch`。
- 探索地图范围已配置为 `40m x 40m x 2m`，高度上限 `2.0m`。
- RViz 默认显示飞机位置、VINS 路径、规划轨迹和彩色点云。
- RViz 已默认开启累计地图 `/sdf_map/occupancy_all_1`，同时保留局部地图 `/sdf_map/occupancy_local_1`。
- frontier/viewpoints 文本显示默认关闭，避免点云图上出现大量数字。
- 明确后续原则：默认不修改 `.cpp/.h/.hpp` 源码，优先通过 launch、xml、yaml、rviz、sh 和运行参数调整。


作者留言2026.5.8.20.53：该项目为实机起飞前的程序调试，完成1.点云图设置2.基于浙大fastdrone250框架部署RACER单机探索算法

## 当前实机运行顺序

每条命令建议单独开一个终端：

```bash
cd ~/EFUEL
source devel/setup.bash
sh shfiles/rspx4.sh
```

```bash
cd ~/EFUEL
source devel/setup.bash
rosrun rosmsg_tcp_bridge odom_to_pose
```

```bash
cd ~/EFUEL
source devel/setup.bash
rostopic echo /vins_fusion/imu_propagate
```

```bash
cd ~/EFUEL
source devel/setup.bash
roslaunch px4ctrl run_ctrl.launch
```

```bash
cd ~/EFUEL
source devel/setup.bash
roslaunch exploration_manager exploration.launch
```

```bash
cd ~/EFUEL
source devel/setup.bash
roslaunch exploration_manager rviz.launch
```

```bash
cd ~/EFUEL
source devel/setup.bash
sh shfiles/record.sh
```

```bash
cd ~/EFUEL
source devel/setup.bash
sh shfiles/takeoff.sh
```

探索节点进入 `WAIT_TRIGGER` 后，用 RViz 的 `2D Nav Goal` 触发，或发布 `/move_base_simple/goal`。

## 建图检查

```bash
rostopic hz /camera/depth/image_rect_raw
rostopic hz /sdf_map/occupancy_local_1
rostopic hz /sdf_map/occupancy_all_1
rostopic echo -n 1 /sdf_map/occupancy_all_1/width
```

判断标准：

- `/camera/depth/image_rect_raw` 有稳定频率。
- `/sdf_map/occupancy_local_1` 有稳定频率。
- `/sdf_map/occupancy_all_1` 有稳定频率且 `width > 0`。
- RViz 中累计地图随探索持续保留，局部地图随飞机当前位置更新。

## 注意事项

- `early` 是 RACER 原始 A* 搜索超时调试输出，不是新增错误。
- `WAIT_TRIGGER` 表示等待触发目标，不是卡死。
- `.gitignore` 已排除 `build/`、`devel/`、日志、缓存和临时文件；源码、launch、RViz、脚本、固件和机械文件已纳入 Git。
- 如果后续确实必须修改源码，需要先说明原因、影响范围和替代方案，再确认执行。
